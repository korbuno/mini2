<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.omp.repository.mapper.EvaluationMapper">
	
	<resultMap id="boardMap" type="EvaluationBoard">
		<result column="board_no" property="boardNo" />
		<result column="title" property="title" />
		<result column="writer" property="writer" />
		<result column="content" property="content" />
		<result column="reg_date" property="regDate" />
		<result column="category_no" property="categoryNo" />
		<result column="member_no" property="memberNo" />
	</resultMap>
	
	<insert id="insertEvaluation" parameterType="EvaluationBoard">
 		insert into board(
			board_no, 
			title, 
			writer, 
			contents,
			reg_date,
			category_no,
			member_no
		) values ( 
			board_no.nextval, 
		    #{title, jdbcType=VARCHAR}, #{writer, jdbcType=VARCHAR}, #{contents, jdbcType=VARCHAR}, 
		    #{regDate, jdbcType=DATE},#{categoryNo, jdbcType=INTEGER}, #{memberNo, jdbcType=INTEGER}
		)
	</insert>
	
	<update id="modifyEvaluation" parameterType="EvaluationBoard">
	   update board 
	      set title = #{title, jdbcType=VARCHAR}, 
	          contents = #{contents, jdbcType=VARCHAR}, 
	          writer = #{writer, jdbcType=VARCHAR} 
	    where board_no = #{boardNo, jdbcType=INTEGER} and category_no = #{categoryNo, jdbcType=INTEGER}
	</update>
	
	<delete id="deleteEvaluation" parameterType="int">
		delete
		  from board
	  	 where board_no = #{boardNo, jdbcType=INTEGER} and category_no = #{categoryNo, jdbcType=INTEGER}
	</delete>
	
	<select id="detailEvaluation" parameterType="int" resultMap="boardMap">
		select * 
			from board
		   where board_no= #{boardNo, jdbcType=INTEGER} and category_no = #{categoryNo, jdbcType=INTEGER}
	</select>
	
	<select id="listEvaluation" resultMap="boardMap">
		select *
			from board 
<!-- 		select b.board_no, b.title, b.writer, b.contents, b.reg_date, s.score
			from board b, score s
				where {boardNo, jdbcType=INTEGER} = {boardNo, jdbcType=INTEGER) -->
	</select>
</mapper>






























